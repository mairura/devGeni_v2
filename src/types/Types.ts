export interface NavType {
  name: string;
  url: string;
  active?: boolean;
}

export interface InBoxCardProps {
  time: string;
}

export interface ContactType {
  name: string;
  msg: string;
  active?: boolean;
}
